type FormValue = {
  appId: string;
  appSecret: string;
  chatId: string;
  chatName: string;

  sendConfigArray: {
    appId: string;
    appSecret: string;
    chatId: string;
    chatName: string;
  }[];
};

export type { FormValue };
